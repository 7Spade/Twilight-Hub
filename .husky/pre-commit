#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 更新項目結構文檔
echo "🔄 更新項目結構文檔..."
npm run docs:update

# 檢查是否有文檔變更
if git diff --staged --name-only | grep -q "docs/project-structure.md"; then
  echo "📝 項目結構文檔已更新，將自動添加到提交中"
  git add docs/project-structure.md
fi

echo "✅ 文檔更新完成"